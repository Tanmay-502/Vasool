# Public backend URL used by the browser.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Demo-only API key for protected POST endpoints. For real production,
# prefer a proper identity provider/session rather than exposing a long-lived key in a browser bundle.
NEXT_PUBLIC_VASOOL_API_KEY=replace_with_demo_key
